'use client';import {useState} from 'react';import SiteHeader from '@/components/SiteHeader';import {getSupabase} from '@/lib/supabase';export default function Login(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');async function submit(){const s=getSupabase();if(!s){setMsg('Supabase is not configured.');return}const {error}=await s.auth.signInWithOtp({email});setMsg(error?error.message:'Check your email for the sign-in link.')}return <><SiteHeader/><main className="mx-auto max-w-md px-5 py-16"><div className="rounded-3xl bg-white p-8 shadow-sm"><h1 className="text-3xl font-black">Sign in</h1><p className="mt-2 text-sm text-slate-500">Use an email magic link.</p><input value={email} onChange={e=>setEmail(e.target.value)} type="email" placeholder="you@example.com" className="mt-6 w-full rounded-xl border border-slate-200 p-3"/><button onClick={submit} className="mt-3 w-full rounded-xl bg-slate-950 px-4 py-3 font-bold text-white">Send magic link</button>{msg&&<p className="mt-4 text-sm text-slate-600">{msg}</p>}</div></main></>}
