'use client';import {useState} from 'react';import SiteHeader from '@/components/SiteHeader';export default function AskIlm(){const [q,setQ]=useState('');const [sent,setSent]=useState(false);return <><SiteHeader/><main className="mx-auto max-w-4xl px-5 py-16"><div className="rounded-[2rem] bg-slate-950 p-8 text-white md:p-12"><p className="font-bold text-indigo-300">🤖 ASK ILM</p><h1 className="mt-2 text-4xl font-black">Your learning assistant</h1><p className="mt-4 max-w-2xl text-slate-300">Ask about Maths, Biology, Physics, Chemistry or Computer Science. Ask Ilm is designed to explain concepts, give hints and encourage learning rather than simply doing homework.</p><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="What would you like to understand?" className="mt-8 min-h-36 w-full rounded-2xl border border-white/10 bg-white/10 p-5 text-white outline-none placeholder:text-slate-500"/><button onClick={()=>setSent(true)} className="mt-4 rounded-2xl bg-white px-6 py-3 font-extrabold text-slate-950">Ask Ilm ✨</button>{sent&&<div className="mt-6 rounded-2xl bg-white/10 p-5 text-slate-200">AI provider integration is intentionally kept separate from the public browser bundle. Connect your server-side AI endpoint next to enable live answers.</div>}</div></main></>}
